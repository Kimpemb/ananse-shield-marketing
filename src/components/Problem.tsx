export default function Problem() {
  return (
    <section className="px-8 py-20 md:px-16">
      <p className="font-mono text-sm text-dusk">the problem</p>
      <h2 className="mt-3 max-w-xl font-display text-3xl font-semibold text-warm md:text-4xl">
        Cameras record. They don't respond.
      </h2>
      <p className="mt-4 max-w-xl font-body text-warm/70">
        Most footage is only useful after something has already gone wrong. Ananse Shield
        acts while it's happening — with a real person confirming before anyone is alarmed
        or anyone is dispatched.
      </p>
    </section>
  )
}